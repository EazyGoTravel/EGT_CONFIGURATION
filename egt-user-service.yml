eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetchRegistry: true
    register-with-eureka: true
  instance:
    hostname: localhost
    preferIpAddress: false 

server:
  port: 8091
  

