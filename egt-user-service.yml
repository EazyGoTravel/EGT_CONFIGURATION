eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/
    fetchRegistry: true
    register-with-eureka: true
  instance:
    hostname: local
    preferIpAddress: false 

server:
  port: 8091
  

